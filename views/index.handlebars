<div class="header">
<h1>Watch More Movies!!</h1>
<div class="addButton">
<h2>Add a Movie</h2>
    <form action="/api/burgers" method="POST">
      <input type="text" id="name" name="burger_name" placeholder="Movie Name">
      <button type="submit" class="addBurger">Add The Movie</button>
    </form>
  </div>
</div>


<main>
  <div class="container">
    <h2>Movies you want to watch!</h2>

        <ul>
{{#each burgers}}
{{#unless this.devoured}}
<div>
  <form action="burgers/{{this.id}}" method="PUT">
    <button type="submit" class="change-eaten" data-id="{{id}}" data-neweaten="{{devoured}}">{{this.burger_name}}</button>
  </form>
  </div>
{{/unless}}
{{/each}}
</ul>
</div>

<div class="second">
<h2>Seen it!</h2>

  {{#each burgers}}
  {{#if this.devoured}}
  <div class="devoured">
    <div>
      {{this.burger_name}}
    </div>
  </div>
  {{/if}}
  {{/each}}
</div>
</main>



